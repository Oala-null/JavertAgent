## 1. 回归测试

- [x] 1.1 增加同 attempt 重复与增量轮询测试，证明每个 v2 run 只投影一次
- [x] 1.2 增加 v3 缓存复用、v2/v3 隔离和新 attempt 隔离测试
- [x] 1.3 增加同 attempt 并发单飞、有界 LRU 与匿名日志测试

## 2. 缓存实现

- [x] 2.1 实现按 `api_version + projection_scope + run_id` 的深拷贝有界 LRU，并覆盖历史回放快照
- [x] 2.2 实现同 attempt 惰性单飞锁，并让 v2 只构建缓存缺失的 cards
- [x] 2.3 让 v3 复用 v2 cards 并缓存逐收费行投影，保持旧版字段隔离
- [x] 2.4 为 v2/v3 匿名出口日志增加缓存命中、缺失和构建耗时

## 3. 验证与交付

- [x] 3.1 运行 2C 直接测试、Web 组合测试、完整套件和严格 OpenSpec 校验
- [x] 3.2 更新 v3 对接文档、架构/变更记录和 62 部署回滚记录
- [x] 3.3 备份并部署 62，重启后验证健康、环境和大结果连续轮询耗时
- [x] 3.4 仅暂存本 change 的代码、测试、文档和 OpenSpec 后创建 commit
