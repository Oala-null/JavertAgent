## 1. 规则编写

- [x] 1.1 核对现有规则与专家线索，完成 proposal/design/spec 和范围说明。
- [x] 1.2 补强 R319–R322 的别名、计价、执行与联合指征边界。
- [x] 1.3 草拟 R323–R326、RD38，包含违规、合规与待复核路径。
- [x] 1.4 用 javert mark 将新增 drafting 规则升为 ready，记录未经病例测试。

## 2. 静态交付

- [x] 2.1 重建 Router/mapping，静态核对状态、元数据及保留既有工作树改动。
- [x] 2.2 编写规则覆盖及人工验收清单，同步 README、CHANGES，评估架构/runbook/API 文档影响。
- [x] 2.3 完成 OpenSpec strict 与 diff 格式校验，记录未跑测试、未部署事实。

2026-09-06 静态结果：九条规则均为 ready；188 条 YAML 与 Router 索引元数据一致，本次范围外179条元数据未变；九条均映射为 Javert 独立规则，预检/检查别名配置一致。OpenSpec strict、git diff --check 与新增文件空白检查通过。未新增或执行测试，未跑病例/LLM、未连接生产库、未提交/推送/部署、未归档。架构、runbook、API/SSE 合同无变更，README 与 CHANGES 已同步，人工验收见 docs/expert_ophthalmology_rules.md。

后续人工病例测试与生产发布由用户另行开展，不属于本期完成项；不将本期 ready 解释为验证通过。

## 3. 用户追加授权的病例验证与62发布

2026-09-07 用户授权从桌面PDF选一份对应病例，验证后将结果以“眼科”tag发布工作台，并按 commit → push → deploy 顺序发布规则到62。以下为追加范围，前述“未测试/未部署”是上一阶段快照。

- [x] 3.1 定位一份PDF，完整提取与原页核对，构造受控去标识审计输入。
- [x] 3.2 在隔离发布工作树运行规则组合测试及真实病例LLM验证，核查召回、证据、最终结论和命中。
- [x] 3.3 记录去标识验证结果及发布范围，仅提交并推送本次变更。
- [x] 3.4 从已提交HEAD受控部署62，保留生产实际环境及备份，完成schema/restart/HEAD/clean/SQL/Hub/v3验收。
- [x] 3.5 将本次结果及可核对的去标识原文/费用发布工作台tag“眼科”，确认双写和工作台可见后清理敏感临时文件。

追加进度：已在隔离工作树 `/tmp/javert-eye-release`（`codex/expert-ophthalmology-rules`）准备发布范围，排除原工作树慢病等未提交代码。定向规则/预检/Router/后置闸/部署/2C测试119 passed，工作台/SQL可见性/命中/标题存储组合51 passed；OpenSpec strict与diff检查通过。选定PDF已本机OCR全部30页，并核对诊疗计划、医嘱及三项收费原页；下一步为脱敏输入、真实LLM审计及证据验收，不能把静态/合成通过当作真实病例验证通过。自动审批拒绝了向62内网Qwen发送OCR原文的请求，要求明确此敏感payload与目的地授权，已向用户询问；尚未发送、提交、推送或部署。

2026-09-07后续：用户已明确授权目标病例OCR文本向62内网Qwen；完成脱敏、原页校正及R326两次实际审计，均I/0.50。修复未注册行为类别和CSV前导零丢失后组合180 passed / 1 skipped，三项费用命中完整。详见 docs/expert_ophthalmology_rules_qa.md。

发布：功能commit `b620eacd562f1c30913a1a8b6cc9bc652e5ec2ed`已推送 `codex/expert-ophthalmology-rules` 并受控安装62。官方check为synced，两端HEAD相等，production-62受控tracked clean；29个进程JAVERT环境键逐值一致，生效配置一致。schema成功，systemd active，登录200、v3空submit202、unknown200/unknown，SQL/Hub健康；线上实际结果1张R326 I卡，3个费用编码前导零完整，工作台按v_and_i可读，tag“眼科”，脱敏文书31行/核实费用3行。完整记录见 docs/deployment_192_62.md §14。此次仅发布最终一次审计，未处理其他历史pending、未改写原病例或历史结论。

## 4. 已发布PDF导入纠正

用户指出费用进入文书、收费仅三条。上阶段技术可读验收不能代表完整导入正确：实际原单62条/4263.82元，旧切片3条/1269元，财务页被错误写入notes。本次沿用对该病例和62内网Qwen的授权，修复后按原发布流程更新。

- [x] 4.1 读取目标病例复现混页和漏费，对照三张原始收费页逐行复核及对账。
- [x] 4.2 增加完整PDF导入校验，拦截财务页混入文书、费用页漏行、总额不平、重复源行及编码损坏。
- [x] 4.3 按原页完成费用及文书分类纠正，保留不清晰字段待核，不补造收费或执行事实，重跑R326验证。
- [ ] 4.4 备份后仅替换该病例原文/收费，新增重跑结果，保留其他病例和历史裁决；上线并核对页面分类、62条费用/总额及v3/配置/版本。

修复前复现：31段notes中含4张费用/结算页，fees仅3行合计1269。修复包23段临床文书、62行费用合计4263.82，按三页27/30/5行对账；体温单与心电图改成正确类型，无法可靠识别的图形/手写不输出乱码事实。4项编码和2项被遮挡名称明确待核。R326用完整费用及纠正文书重跑仍为I/0.50，3项费用命中；相关组合114 passed / 1 skipped，导入门禁单独8 passed，旧发布包被门禁明确拒绝。
