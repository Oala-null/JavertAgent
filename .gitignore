# ============================================================
# 隐私数据 (PHI — 真实患者数据, 绝不入 git)
# ============================================================
# data/ 全部排除; 仅保留 router 运行所需的非 PHI 字典 json
data/*
!data/router/
data/router/*
!data/router/*.json

# 外部医院导入快照 (case_notes / shi_fee / shi_zd / shi_ss + .loaded.env)
data_import/

# 患者数据测试集 (费用 / 文书 / 诊断 / 手术 / 检验)
test1/
test2/
test3/
test4/
# 同类测试集复制目录也可能含真实患者数据（例如 `test4 copy/`）
/test[0-9]*/

# 审计结果与日志 (含患者裁决)
output/

# 文档中的真实审核记录 (含患者 ID + 临床批注)
docs/javert_vio_review.csv

# 本机静态参考资料（保留在工作区，不作为产品版本依赖）
/docs/243部署手册.md
/docs/templates/260611医保基金监管规则框架总表.xlsx
/截至20260701诊疗项目.xls
/docs/*算力配置单*

# ============================================================
# 公司既有 Java 监管引擎源码 (内部专有, 不入公开仓库)
# ============================================================
规则引擎代码/

# ============================================================
# 凭证 / 密钥
# ============================================================
.env
.env.*
243_config.env
configs/llm.local.yaml
*.local.yaml

# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/

# uv / venv
.venv/
venv/
uv.lock.bak

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/
.cache/

# Editor
.vscode/
.idea/
*.swp
*.swo
~$*

# OS
.DS_Store
Thumbs.db

# ============================================================
# 红区补漏 (2026-07-05 进院前扫描)
# ============================================================
# data-hub 流B 反向取数快照 (真实患者数据, PHI; 通配盖住 _szx2 等批次变体)
data_import_hub*/
# onboarding GUI 上传目录 (现场医院 CSV, PHI)
_uploads/
# aider 工具产物 (含代码索引缓存)
.aider*
# 软著申报等打包文档 (可再生二进制)
*.docx
